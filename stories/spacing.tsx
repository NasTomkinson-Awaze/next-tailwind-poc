import { Explainer } from "./explainer";
export const Spacing = () => {
  return (
    <>
      <Explainer
        title="Spacing"
        description="The principle behind spacing is that there are a set number of values, all of which based on a scale which can be applied to all spacing properties. Such as: Padding, Margin, Gap, Width, Height, etc.."
        usage="{p | m | gap | w | h }-{ x(int) }"
      ></Explainer>
    </>
  );
};
